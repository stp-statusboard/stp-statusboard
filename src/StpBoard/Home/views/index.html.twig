{% extends 'layout.html.twig' %}

{% block body %}
    <div class="row">
        {% for item in items %}
            <div class="boardItem col-md-{{ item.width }}" data-src="{{ item.url }}"
                 data-refresh="{{ item.refresh }}"></div>
        {% endfor %}
    </div>
{% endblock %}
